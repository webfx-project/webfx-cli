<?xml version="1.0" encoding="UTF-8"?>
<!-- File managed by WebFX (DO NOT EDIT MANUALLY) -->
<module>

    <!-- Configuring logging (must be in first position) -->
    <inherits name="com.google.gwt.logging.Logging"/>
    <set-property name="gwt.logging.enabled" value="TRUE"/>
    <set-property name="gwt.logging.logLevel" value="FINEST"/>
    <set-property name="gwt.logging.consoleHandler" value="ENABLED"/>

    <!-- Speeding up GWT compilation and execution times -->
    <set-property name="user.agent" value="safari"/> <!-- to speed up GWT compilation - this single permutation is now working ok in all modern browsers -->
    <set-configuration-property name="user.agent.runtimeWarning" value="false"/> <!-- removing irrelevant warning in other browsers -->
    <set-property name="jre.checks.checkLevel" value="MINIMAL"/> <!-- to speed up execution time -->

    <!-- Specify the app entry point class. -->
    <entry-point class="dev.webfx.platform.boot.spi.impl.gwt.GwtApplicationBooterProvider"/>

    <super-source path="super"/>
    <super-source path="" includes="java/io/"/>
    <super-source path="" includes="java/lang/"/>
    <super-source path="" includes="java/security/"/>
    <super-source path="" includes="java/text/"/>
    <super-source path="" includes="java/util/"/>

    <public path="public"/>

</module>