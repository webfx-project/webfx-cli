<?xml version="1.0" encoding="UTF-8"?>
<project>

    <exported-packages>
        <source-packages/>
        <resource-package>dev.webfx.tool.cli.subcommands</resource-package>
        <resource-package>dev.webfx.tool.cli.jdk</resource-package>
        <resource-package>dev.webfx.tool.cli.templates</resource-package>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <properties>
        <java.version>13</java.version>
    </properties>

    <required-libraries>

        <webfx-library artifact="dev.webfx:webfx-lib-reusablestream:0.1.0-SNAPSHOT"/>
        <library artifact="info.picocli:picocli:4.6.3" name="info.picocli"/>
        <library artifact="org.apache.maven.shared:maven-invoker:3.2.0"/>

    </required-libraries>

    <maven-pom-manual>
        <properties>
            <!-- Java 13 is required because of the use of FileSystems.newFileSystem() in the code -->
            <java.version>13</java.version>
            <webfx.mainClass>dev.webfx.tool.cli.WebFx</webfx.mainClass>
            <!-- disabling classes and sources dependencies unpack to avoid including them again in the jar -->
            <plugin.dependency.param.unpack-classes.phase>none</plugin.dependency.param.unpack-classes.phase>
            <plugin.dependency.param.unpack-sources.phase>none</plugin.dependency.param.unpack-sources.phase>
        </properties>

        <build>
            <plugins>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                </plugin>

            </plugins>
        </build>

    </maven-pom-manual>

</project>
